import { Helmet } from 'react-helmet-async';

const Dashboard = () => {
    return (
        <div>
            <Helmet>
                <title>Parcel Tracker | Dashboard</title>
            </Helmet>
            Hi from Dashboard
        </div>
    );
};

export default Dashboard;